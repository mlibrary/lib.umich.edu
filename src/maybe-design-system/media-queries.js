import { MEDIA_QUERIES as MQ } from '@umich-lib/core'

const MEDIA_QUERIES = {
  XL: '(min-width: 1200px)',
  L: '(min-width: 920px)',
  M: '(min-width: 720px)',
  S: MQ.LARGESCREEN,
}

export default MEDIA_QUERIES
