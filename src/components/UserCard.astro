---
/**
 * UserCard Component
 * Converted from src/components/user-card.js
 *
 * Displays a contact card with photo, name link, title, phone, and email.
 */
import StaffPhotoPlaceholder from './StaffPhotoPlaceholder.astro';
import DrupalImage from './DrupalImage.astro';
import Link from '../reusable/Link.astro';

interface Props {
  name: string;
  to: string;
  image?: {
    alt: string;
    url: string;
    width?: number;
    height?: number;
  };
  title?: string;
  phone?: string;
  email?: string;
}

const { name, to, image, title, phone, email } = Astro.props;
---

<section class="user-card">
  <div class="user-photo-container">
    {image?.url ? (
      <DrupalImage
        src={image.url}
        alt={image.alt || ''}
        class="user-photo"
        loading="lazy"
        width={image.width || 640}
        height={image.height || 853}
        maxWidth={640}
      />
    ) : (
      <StaffPhotoPlaceholder />
    )}
  </div>
  <div class="user-info">
    <Link href={to} kind="description">{name}</Link>
    {title && <p class="user-title">{title}</p>}
    {phone && (
      <p>
        <Link href={`tel:${phone}`}>{phone}</Link>
      </p>
    )}
    {email && (
      <p>
        <Link href={`mailto:${email}`}>{email}</Link>
      </p>
    )}
  </div>
</section>

<style>
  .user-card {
    display: flex;
    margin: 1.5rem 0; /* SPACING.L */
  }

  .user-photo-container {
    aspect-ratio: 3 / 4;
    background-color: var(--color-blue-100);
    border-radius: 2px;
    flex-shrink: 0;
    overflow: hidden;
    width: 75px;
  }

  /* override DrupalImage's inline aspect-ratio so the wrapper fills the flex-stretched container */
  .user-photo-container :global(.gatsby-image-wrapper) {
    aspect-ratio: unset !important;
    height: 100%;
  }

  .user-photo-container :global(svg) {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .user-photo {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 2px;
  }

  .user-info {
    margin-left: 1rem; /* SPACING.M */
  }

  .user-title {
    margin: 0;
    max-width: 38rem;
  }

  .user-info p {
    margin: 0;
  }
</style>
